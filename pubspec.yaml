name: ez_credosafe_flutter
description: VS Corporate Lending mobile app (Phase 1 MVP)
publish_to: "none"
version: 1.0.0+1
environment:
  sdk: ">=2.18.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.0.5
  dio: ^5.2.1
  flutter_secure_storage: ^8.0.0
  file_picker: ^5.2.5
  image_picker: ^0.8.7+5
  intl: ^0.18.1
  connectivity_plus: ^4.0.1
  google_fonts: ^6.1.0
  font_awesome_flutter: ^10.6.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
